---
import Layout from "../layouts/Layout.astro";
import { AiChat } from "../components/ai/AiChat";
const user = (Astro.locals as any).user;
if (!user) return Astro.redirect("/login");
---

<Layout title="KI-Assistent — Hatches" active="ai">
  <AiChat client:load />
</Layout>
