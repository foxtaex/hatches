---
import "../styles/global.css";
<<<<<<< Updated upstream
=======
import "@fortawesome/fontawesome-free/css/all.min.css";
import { HatchesLogo } from "../components/brand/HatchesLogo";
>>>>>>> Stashed changes

export interface Props {
  title?: string;
  active?: "board" | "docs" | "notes" | "websites" | "integrations" | "admin";
}

const { title = "Hatches", active } = Astro.props;
const user = (Astro.locals as any).user;

const nav = [
  { href: "/board", label: "Board", key: "board", icon: "fa-solid fa-table-columns" },
  { href: "/docs", label: "Docs", key: "docs", icon: "fa-solid fa-book" },
  { href: "/notes", label: "Notizen", key: "notes", icon: "fa-solid fa-note-sticky" },
  { href: "/websites", label: "Websites", key: "websites", icon: "fa-solid fa-globe" },
  { href: "/integrations", label: "Integrationen", key: "integrations", icon: "fa-solid fa-plug" },
];
---

<!doctype html>
<html lang="de">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title}</title>
<<<<<<< Updated upstream
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.7.2/css/all.min.css" integrity="sha512-Evv84Mr4kqVGRNSgIGL/F/aIDqQb7xQ2vcrdIwxfjThSH8CSR7PBEakCr51Ck+w+/U6swU2Im1vVX0SVk9ABhg==" crossorigin="anonymous" referrerpolicy="no-referrer" />
=======
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet" />
    <style>
      :root { --mint: #3CC79A; --mint-dim: rgba(60,199,154,0.12); }
      body { font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'SF Pro Text', sans-serif; -webkit-font-smoothing: antialiased; -moz-osx-font-smoothing: grayscale; }
    </style>
>>>>>>> Stashed changes
  </head>
  <body class="bg-zinc-950 text-zinc-100 min-h-screen flex flex-col">
    <header class="border-b border-zinc-800 bg-zinc-900">
      <nav class="flex items-center gap-1 px-4 h-12">
        <span class="font-bold text-zinc-300 mr-4 text-sm tracking-widest uppercase">
          <i class="fa-solid fa-screwdriver-wrench mr-1.5 text-zinc-500"></i>Hatches
        </span>

        {nav.map(({ href, label, key, icon }) => (
          <a
            href={href}
            class={`flex items-center gap-1.5 px-3 py-1.5 rounded text-sm font-medium transition-colors ${
              active === key
                ? "bg-zinc-700 text-white"
                : "text-zinc-400 hover:text-zinc-100 hover:bg-zinc-800"
            }`}
          >
            <i class={`${icon} w-3.5 text-center`}></i>
            {label}
          </a>
        ))}

        {user?.isAdmin && (
          <a
            href="/admin"
            class={`flex items-center gap-1.5 px-3 py-1.5 rounded text-sm font-medium transition-colors ml-1 ${
              active === "admin"
                ? "bg-zinc-700 text-white"
                : "text-zinc-600 hover:text-zinc-300 hover:bg-zinc-800"
            }`}
          >
            <i class="fa-solid fa-shield-halved w-3.5 text-center"></i>
            Admin
          </a>
        )}

        <div class="ml-auto flex items-center gap-3">
          {user && (
            <>
              <div class="flex items-center gap-2 text-sm text-zinc-500">
                <div class="w-6 h-6 rounded-full bg-zinc-700 flex items-center justify-center text-xs font-bold text-zinc-300">
                  {(user.displayName || user.username).charAt(0).toUpperCase()}
                </div>
                <span>{user.displayName || user.username}</span>
              </div>
              <button
                id="logout-btn"
                class="flex items-center gap-1.5 text-xs text-zinc-600 hover:text-zinc-300 transition-colors"
                title="Abmelden"
              >
                <i class="fa-solid fa-right-from-bracket"></i>
                Abmelden
              </button>
            </>
          )}
        </div>
      </nav>
    </header>

    <main class="flex-1 flex flex-col overflow-hidden">
      <slot />
    </main>

    <script>
      document.getElementById("logout-btn")?.addEventListener("click", async () => {
        await fetch("/api/auth/logout", { method: "POST" });
        window.location.href = "/login";
      });
    </script>
  </body>
</html>